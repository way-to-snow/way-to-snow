<%= simple_form_for([ resort, question ]) do |form| %>
  <%= form.input :question, as: :text %>
  <%= form.button :submit %>
<% end %>

<% if flash[:alert] %>
  <div class="alert"><%= flash[:alert] %></div>
<% end %>
